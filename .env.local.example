DATABASE_URL=postgres://booking:booking@localhost:5433/booking

# Optional — leave both unset locally. Turnstile verification is skipped when
# TURNSTILE_SECRET_KEY is absent (see lib/turnstile.ts), so booking works without them.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
